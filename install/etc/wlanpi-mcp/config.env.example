WLANPI_CORE_URL=https://localhost:31415
# CA for wlanpi-core's TLS listener (self-signed, generated by wlanpi-core).
WLANPI_CORE_CA=/etc/nginx/ssl/self-signed-wlanpi.cert
# Fallback wlanpi-core JWT for stdio transport only. In SSE/daemon mode leave
# empty: clients must send their own token as 'Authorization: Bearer <token>'.
WLANPI_CORE_TOKEN=
# Loopback-only: nginx fronts the SSE daemon on 8766 with TLS, so clients
# connect to https://<wlanpi>:8766/sse. 8767 is the internal upstream.
WLANPI_MCP_HOST=127.0.0.1
WLANPI_MCP_PORT=8767
LOG_LEVEL=INFO
# Allow MCP clients to reboot/shut down the device. Set false to disable.
ALLOW_POWER_CONTROL=true